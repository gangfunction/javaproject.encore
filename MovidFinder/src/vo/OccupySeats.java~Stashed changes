package vo;

import dto.movieDto;

public class occupySeats {
    public occupySeats() {
        if (movieDto.getPointer() == 1) {
            new seatEdit();
        } else {
            System.out.println("잘못된 입력입니다.10");
        }
    }
}
